% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_preprocessing.R
\name{preprocess_methylation_data}
\alias{preprocess_methylation_data}
\title{Preprocess methylation data for EnsembleAge prediction}
\usage{
preprocess_methylation_data(
  dat0sesame,
  samps,
  min_coverage = 0.8,
  handle_missing = "impute",
  verbose = TRUE
)
}
\arguments{
\item{dat0sesame}{Data frame containing methylation data with CGid column}

\item{samps}{Data frame containing sample information}

\item{min_coverage}{Minimum probe coverage required (default: 0.8)}

\item{handle_missing}{Character string indicating how to handle missing values:
"remove" (remove samples/probes with too many NAs), "impute" (simple mean imputation),
or "keep" (keep as is). Default: "impute"}

\item{verbose}{Logical indicating whether to print progress messages (default: TRUE)}
}
\value{
List containing processed data and metadata
}
\description{
This function preprocesses methylation data to ensure it's in the correct format
for age prediction. It handles missing values, filters probes, and validates the data.
}
\examples{
\dontrun{
processed <- preprocess_methylation_data(methylation_data, sample_info)
prediction_results <- predictAgeAndAgeAcc(processed$data, processed$samples)
}
}
